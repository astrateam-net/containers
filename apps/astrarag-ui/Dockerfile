# syntax=docker/dockerfile:1
# check=skip=InvalidDefaultArgInFrom
ARG VERSION

# Stock LlamaCloud frontend, republished under our registry. UI only, no license
# validator in this image (verified: no "llamaindex-byoc" anchor).
FROM docker.io/llamaindex/llamacloud-frontend:${VERSION}
