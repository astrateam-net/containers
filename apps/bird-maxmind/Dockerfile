# Placeholder — the real Dockerfile is in the source repo.
#
# The build context is fetched from ${SOURCE}.git#v${VERSION} (see docker-bake.hcl),
# which supplies its own Dockerfile. This file is kept only to satisfy
# repository conventions (apps/<name>/Dockerfile is expected to exist).
#
# Source: https://github.com/mrkhachaturov/bird
FROM scratch
